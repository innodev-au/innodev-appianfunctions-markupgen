function.markupgen_html_newtextpart.description=Produces an HTML part, taking the text and escaping it if necessary.
function.markupgen_html_newtextpart.param.text.description=Text content which will be HTML-escaped if necessary.
